services:
  api:
    ports:
      - "127.0.0.1:${API_PORT:-8010}:8000"

  osint:
    ports:
      - "127.0.0.1:${OSINT_PORT:-8081}:8080"

  qdrant:
    ports:
      - "127.0.0.1:6335:6333"

  redis:
    ports:
      - "127.0.0.1:${REDIS_PORT:-6380}:6379"

  caddy:
    ports:
      - "80:80"
      - "443:443"
